# 판정 규칙 — 수식 전문

착한주유소 한 곳이 **적합 · 근접 · 초과 · 과거 미신고 · 가격정보 없음** 중
무엇을 받는지, 관리 화면에서 고를 수 있는 **네 가지 방식**별로 식을 전부 적는다.

이 문서는 코드가 원본이다. 다음 파일과 1:1로 맞춰 두었다.

| 구간 | 파일 |
| --- | --- |
| 모집단·순위·계수·색 | `src/lib/signal.ts` |
| 기준지역 분리 | `src/lib/region.ts` |
| 보정 판정 | `src/lib/adjust.ts` |
| 기준선 생성 | `scripts/build-baseline.ts` |
| 판정 실행 | `scripts/aggregate.ts` |
| 시계열·기준 충족 | `src/lib/history.ts` |
| 화면 전환 | `client/src/lib/board.ts` · `client/src/components/Admin.tsx` |

기준일 예시는 모두 **2026-09-06 판매가**(전국 10,210건)이다.

---

## 0. 한눈에

```
                     ┌─ ① 전처리 ────────────────────────────────┐
전국 원본 R_t  ────▶ │ 유효 관측만 남긴다 · 기준지역 s 로 가른다  │
(오피넷 1만 건)      │ 합계 x = 휘발유 + 경유                    │
                     └──────────────┬────────────────────────────┘
                                    │
                     ┌──────────────▼────────────────────────────┐
                     │ ② 모집단 P_s · 서로 다른 값 U_s           │
                     │   조밀순위 ρ · 커트라인 B_s · 계수 c      │
                     └──────────────┬────────────────────────────┘
                                    │
        ┌───────────────────────────┴───────────────────────────┐
        │                                                       │
   ③ 방식 A                                          ③ 방식 B / C / D
   기존 순위                                          보정 (기준선 필요)
   Sig_rank(ρ)                                        Sig_rank(ρ̃) · Sig_drift(δ)
        │                                                       │
        └───────────────────────────┬───────────────────────────┘
                                    │
                     ┌──────────────▼────────────────────────────┐
                     │ ④ 덮어쓰기 — 결측 이력이 있으면 방식과    │
                     │    무관하게 stale / unknown 으로 바꾼다   │
                     └───────────────────────────────────────────┘
```

**④ 가 ③ 을 이긴다.** 네 방식 중 무엇을 고르든 결측 이력이 있는 곳은 색이 같다.

---

## 1. 기호

| 기호 | 뜻 | 코드 |
| --- | --- | --- |
| `t` | 판정 기준일 (YYYYMMDD) | `date` |
| `R_t` | 그날 전국 원본 행 집합 | `raw.rows` |
| `r.g`, `r.d` | 그 행의 휘발유·경유 판매가 (원/L) | `gasoline`, `diesel` |
| `s(r)` | **기준지역** — 순위를 매길 때 견주는 단위 | `basisSido()` |
| `x(r)` | 합계 `r.g + r.d` | `sum` |
| `P_s` | 기준지역 `s` 의 합계 모집단 (다중집합) | `sumBuckets` |
| `U_s` | `P_s` 에서 **서로 다른 값**만 오름차순 | `distinctAsc()` |
| `n_s` | `P_s` 의 크기 — 중복 포함 | `stat.n` |
| `N_s` | 적합 기준 순위 | `greenRankWith()` |
| `f` | 근접 배수 | `rankYellowFactor` |
| `ρ` | 조밀 순위 | `rankOf()` |
| `B_s` | 적합 커트라인 (계수 1 이 되는 값) | `greenBase` |
| `c` | 계수 | `coefficient` |
| `E` | 선정 때 후보가 아니었던 것으로 보이는 주유소 집합 | `baseline.excluded` |
| `δ` | 이탈률 | `drift` |

---

## 2. 전처리

### 2.1 유효 관측

합계는 **두 유종을 모두 파는 곳만** 갖는다. 한 유종만 파는 곳의 합계는 존재하지
않으므로 순위표에 끼울 수 없다.

```
x(r) = r.g + r.d      단, r.g > 0 그리고 r.d > 0
     = 정의 없음       그 밖의 경우 (모집단에서 빠진다)
```

`null` 과 `0` 을 같이 뺀다. 오피넷은 미취급과 미신고를 모두 빈 값으로 주기 때문이다.

### 2.2 기준지역 `s(r)` — 순위를 어디에서 세는가

비교 단위는 **시·도**다. 시·군·구로 쪼개면 주유소가 두세 곳뿐인 곳이 생겨
"관내 1위"가 아무 뜻도 갖지 않는다. 가장 작은 세종도 시·도 단위면 60곳이 넘는다.

다만 **전남광주통합특별시는 둘로 가른다.**

```
              ⎧ "광주"     sido = "전남광주" 이고 sigungu ∈ {광산구, 동구, 서구, 남구, 북구}
s(sido,sgg) = ⎨ "전남"     sido = "전남광주" 이고 그 밖의 시·군
              ⎩ sido       그 밖의 모든 시·도
```

행정구역은 합쳐졌고 오피넷도 합쳐서 내려주지만, **선정은 여전히 광주와 전남을
따로 뽑는다** — 명단 원본을 차수 × 시·도로 세면 매 차수 광주 5곳, 전남 5곳이
따로 잡힌다. 합쳐서 세면 1,030곳 한 모집단에 상위 5자리가 되어 선정 때 열이었던
자리가 다섯으로 줄어든다.

지도 드릴다운이 쓰는 `sido` 는 통합시 그대로 두고, **순위와 커트라인을 낼 때만**
가른다.

### 2.3 모집단

```
P_s = { x(r) : r ∈ R_t,  s(r) = s,  x(r) 정의됨 }
U_s = sort(P_s) 에서 연속 중복을 제거한 오름차순 목록
n_s = P_s 의 크기 (중복 포함)
```

2026-09-06 실측: 충북 `n = 648`, 제주 `n = 189`, 경기 `n = 2,156`.

---

## 3. 순위 · 커트라인 · 계수

### 3.1 적합 기준 순위 `N_s`

```
        ⎧ rankGreenMetro     s ∈ {서울, 경기}     (기본 10)
N_s  =  ⎨
        ⎩ rankGreenDefault   그 밖의 시·도        (기본 5)

근접 기준 순위  Y_s = N_s × f      (기본 f = 2 → 서울·경기 20위, 그 외 10위)
```

서울 384곳·경기 2,156곳(2026-09-06)에 다른 시·도와 같은 5위 잣대를 대면 사실상 아무도
통과하지 못한다. 그래서 두 곳만 10위로 둔다.

> `N_s` 를 고를 때 쓰는 것은 기준지역이 아니라 **오피넷 소재 시·도**(`effSido`)다.
> 갈라지는 것은 전남광주뿐이고 서울·경기는 갈라지지 않으므로 결과는 언제나 같다.

### 3.2 조밀 순위 `ρ`

동점은 같은 등수이고, **등수는 서로 다른 가격의 개수만큼만 존재한다.**
1위가 세 곳 동점이면 다음은 4위가 아니라 **2위**다.

```
ρ(x, U) = |{ u ∈ U : u < x }| + 1
```

구현은 `U` 위의 이분 탐색(`rankOf()`)이라 같은 값을 준다.

### 3.3 커트라인 `B_s`

**서로 다른 값** 기준이다. 조밀 순위와 같은 잣대여야 앞뒤가 맞는다.

```
B_s  = U_s[ min(N_s, |U_s|) ]        ← 1-기반 인덱스, 적합 커트라인
B'_s = U_s[ min(Y_s, |U_s|) ]        ← 근접 커트라인
```

서로 다른 값이 기준 순위보다 적으면 **마지막 값**을 쓴다. 이때는 모두가
`x ≤ B_s` 라 전원이 적합이 된다 — 시·도 단위에서는 실제로 일어나지 않는다.

동점이 많은 시·도에서는 커트라인이 뒤로 밀린다. 서울은 정렬 목록의 열 번째 줄
3,617원이 아니라 **열 번째로 싼 가격** 3,634원이 커트라인이다.

### 3.4 계수 `c`

**커트라인을 1 로 둔다.** 1 이 곧 합격선이라 계수 하나로 판정 근거가 읽힌다.

```
round_4(z) = round(z × 10⁴) / 10⁴

c₀ = round_4( x / B_s )

     ⎧ 1 + 10⁻⁴     x > B_s 이고 c₀ ≤ 1
c  = ⎨ 1 − 10⁻⁴     x < B_s 이고 c₀ ≥ 1
     ⎩ c₀           그 밖의 경우
```

**왜 걸어 잠그나.** 커트라인보다 1원 비싼 곳이 `1.0000` 으로 반올림되면 화면에
"계수 1인데 근접"이 되어 판정 근거가 어긋나 보인다. 1 은 합격선이라는 뜻이므로
커트라인 **이하**일 때만 나와야 한다.

**왜 넷째 자리인가.** 합산 커트라인이 3,600원 언저리라 1원이 0.00028 이다.
셋째 자리면 3,608원과 3,609원이 둘 다 `1.002` 로 찍혀 순위가 다른데도 같아 보였다.

`x` 나 `B_s` 가 없거나 `B_s ≤ 0` 이면 계수는 `null` 이다.

### 3.5 순위 → 색

```
                       ⎧ unknown   ρ 가 없거나 n < 2
Sig_rank(ρ, N, f, n) = ⎪ green     ρ ≤ N
                       ⎨ yellow    N < ρ ≤ N·f
                       ⎩ red       ρ > N·f
```

`n < 2` 를 거르는 이유는 비교 대상이 자기 자신뿐이면 자동으로 1위가 되기
때문이다. 시·도 단위에서는 걸리지 않지만 기준을 시·군·구로 되돌릴 때를 대비해 둔다.

**계수와의 관계.** `|U_s| ≥ N_s` 인 한 다음이 정확히 성립한다.

```
적합 ⟺ ρ ≤ N_s ⟺ x ≤ B_s ⟺ c ≤ 1
근접 ⟺ ρ ≤ Y_s ⟺ x ≤ B'_s
```

---

## 4. 방식 A — 기존 순위 (`judgeMode: "rank"`)

> "오늘 그 시·도에서 몇 위냐"

주유소 `i` 의 합계를 `x_i`, 기준지역을 `s` 라 하면

```
ρ_i     = ρ(x_i, U_s)
signal  = Sig_rank(ρ_i, N_s, f, n_s)
c_i     = clamp(round_4(x_i / B_s))
```

모집단은 **제외분을 빼지 않은 전국 원본 그대로**다. `n` 자리에는 중복을 포함한
`n_s` 가 들어간다.

이 방식이 답하지 못하는 것: 명단 472곳은 아홉 차수에 걸쳐 뽑혔고 차수마다 선정
기준기간이 다르다. 오래전에 뽑힌 차수를 오늘의 상위 N위로 재면 뒤로 밀리는 것이
당연하다 — 잣대가 묻는 질문이 선정 때와 다르기 때문이다. 방식 B~D 가 그 문제를 다룬다.

---

## 5. 보정 판정의 재료 (`data/baseline.json`)

방식 B·C·D 는 모두 기준선이 있어야 한다. `npm run baseline` 이 보관 원본에서 만든다.

### 5.1 그 주유소의 기준 차수

한 주유소가 여러 차수에 걸쳐 다시 뽑히기 때문에(172곳) **마지막 차수**를 쓴다.
최초 차수로 잡으면 9차에 다시 뽑힌 곳을 1차 기준으로 재게 된다 — 마지막으로 세면
9차가 37곳이 아니라 93곳이다.

```
round(i) = station-rounds.json 의 last   (없으면 명단의 round)
w(i)     = windows[ round(i) ]           ← 기준기간 YYYYMM
```

기본 기준기간:

```
1차 ~ 8차 → 202607          9차 → 202608
```

8차·9차는 보관 원본으로 확인했다 — 8차는 7월 기준 충족률 80%(순위 중앙값 3위),
9차는 8월 기준 59% 로 각각 그 달에 몰려 있다. **1~7차는 보관 원본이 7월부터라
확인할 수 없어 7월로 둔 추정치다.** 그 차수들은 '관측을 시작한 7월 대비' 이탈을
재는 셈이라 7월 이전에 이미 올린 몫은 잡히지 않는다 — 실제보다 후하게 나온다.
화면에 그렇게 밝힌다. `data/round-windows.json` 으로 덮어쓸 수 있다.

### 5.2 기준가 `P0` — 그 주유소가 선정 무렵 받던 값

```
D_i,w = { τ ∈ w : x_i(τ) 정의됨 }              ← 그 달 중 두 유종을 모두 판 날

P0_i  = ( 1 / |D_i,w| ) · Σ_{τ ∈ D_i,w} x_i(τ)      (소수 둘째 자리 반올림)
```

`|D_i,w| = 0` 이면 기준선을 만들지 않는다 → 그 주유소는 보정 방식에서 `unknown`.

### 5.3 시장 `M0` · `M1` — 그 사이 시세가 얼마나 움직였나

```
M0_s(w) = median{ x_j(τ) : τ ∈ w,  s(j) = s,  x_j(τ) 정의됨 }    ← 주유소-일 단위 풀링
M1_s(t) = median{ x_j(t) : s(j) = s,  x_j(t) 정의됨 }             ← 그날 한 장
```

**평균이 아니라 중앙값을 쓴다.** 상위 N위 커트라인은 극단 순서통계량이라 싼 쪽
꼬리 몇 곳이 빠지고 드는 것만으로 통째로 움직인다. 중앙값은 그런 것에 꿈쩍하지
않아 "시장이 어디로 움직였는지"를 재는 데 알맞다.

> **비대칭 주의.** `M0` 은 한 달치 주유소-일을 **모두 풀링한** 중앙값이고,
> `M1` 은 하루치 주유소 중앙값이다. 개별 주유소의 월평균을 다시 중앙값 낸 것이
> 아니다. 관측일이 많은 주유소가 `M0` 에 더 크게 반영된다는 뜻인데, 결측이 드물어
> 실제 영향은 미미하다.
>
> `M1` 은 **제외분을 빼지 않은** 전체 모집단에서 낸다 (`marketNow ← sumBuckets`).
> 시장이 어디 있는지를 묻는 값이므로 후보 자격과 무관하다.

### 5.4 제외 추정 `E` — 선정 때 후보가 아니었던 곳

선정이 '그 시·도 상위 N위'인데 뽑히지 않은 주유소 중에 뽑힌 곳보다 싼 곳이
있다면, 그 곳은 **애초에 후보가 아니었다**는 뜻이다.

기준기간을 데이터로 확인한 차수(8차·9차)만 놓고 역산한다.

```
차수 R, 기간 w 에 대해

worst_s(R) = max{ P0_j : j ∈ R,  s(j) = s }        ← 그 차수 그 지역의 최하위 기준가

E_infer(R) = { j : j ∉ 명단,  |D_j,w| > 0,  P0_j < worst_{s(j)}(R) }
```

**순위가 아니라 값으로 견준다.** 조밀 순위에서 동점은 같은 등수라, 최하위와 값이
**같은** 곳은 뽑힐 수도 있었던 자리다. 그런 곳까지 제외로 몰면 실제보다 많이
걷힌다. 그래서 `<` 이지 `≤` 가 아니다.

여기에 제외요청으로 명단에서 빠진 곳을 합친다. 둘은 성격이 다르다 — 역산은
'애초에 후보가 아니었던 곳', 제외요청은 '뽑혔다가 빠진 곳'이다.

```
E = E_infer(8차) ∪ E_infer(9차) ∪ { 제외요청으로 빠진 주유소 }
```

2026-09-06 기준 `|E| = 109`.

### 5.5 보정 모집단

```
P̃_s = { x(r) : r ∈ R_t,  s(r) = s,  r.stationId ∉ E,  x(r) 정의됨 }
Ũ_s = distinct(P̃_s)
B̃_s = Ũ_s[ min(N_s, |Ũ_s|) ]
```

선정이 그 잣대로 이뤄졌으니 견줄 때도 같은 잣대라야 앞뒤가 맞는다.

> `|Ũ_s|` 는 **서로 다른 값의 개수**다. 방식 A 가 `n_s`(중복 포함)를 쓰는 것과
> 다르다. `n < 2` 판정에만 쓰이므로 결과가 갈리지는 않는다.

---

## 6. 방식 B — 보정 · 순위 (`adjusted` + `combine: "rank"`)

> "선정 때 후보에서 빠졌던 곳을 뺀 모집단에서 다시 세면 몇 위냐"

```
ρ̃_i    = ρ(x_i, Ũ_s)
signal = Sig_rank(ρ̃_i, N_s, f, |Ũ_s|)
c̃_i    = clamp(round_4(x_i / B̃_s))
```

방식 A 와 식이 같고 모집단만 다르다. 제외분이 전국 109곳이라 순위가 몇 칸 움직이는
정도이고, 이것만으로 판정이 크게 뒤집히지는 않는다 (적합 94 → 101).

---

## 7. 방식 C — 보정 · 이탈률 (`adjusted` + `combine: "drift"`)

> **선정 시점에 견줘, 그 지역 시장이 오른 만큼을 빼고도 더 올렸는가.**

```
기대가   Ê_i = P0_i × ( M1_s(t) / M0_s(w(i)) )        (소수 둘째 자리 반올림)

이탈률   δ_i = ( x_i − Ê_i ) / Ê_i                    (소수 여섯째 자리 반올림)
```

`δ = 0` 이면 시장이 오른 만큼만 따라 올린 것이고, 양수면 그보다 더 올린 것이다.
유가가 통째로 오르내려도 값이 흔들리지 않는다.

```
               ⎧ green    δ ≤ driftGreen                    (기본 0)
Sig_drift(δ) = ⎨ yellow   driftGreen < δ ≤ driftYellow      (기본 0.01 = 1%)
               ⎩ red      δ > driftYellow
```

**순위를 전혀 보지 않는다.** 그래서 이 방식만 적합이 크게 는다 (94 → 216) —
"지역에서 몇 위냐"가 아니라 "약속을 지켰냐"를 묻기 때문이다.

산출 조건: 기준선이 있고, 그 주유소의 `P0` 가 있고, `x_i` 가 있고, `M0`·`M1` 이
모두 있어야 한다. 하나라도 없으면 `adjusted = null` → 보정 방식에서 `unknown`.

---

## 8. 방식 D — 보정 · 둘 다 (`adjusted` + `combine: "both"`)

> 순위와 이탈률을 **모두** 통과해야 적합. 가장 엄격하다.

```
worse(a, b) = ⎧ a                          a ∉ {green, yellow, red}
              ⎨ b                          b ∉ {green, yellow, red}
              ⎩ green < yellow < red 에서 나쁜 쪽

signal = worse( Sig_rank(ρ̃_i, N_s, f, |Ũ_s|),  Sig_drift(δ_i) )
```

판정 불가(`unknown`·`stale`)는 섞지 않고 그대로 내보낸다 — 값이 없어서 못 재는
것과, 재 봤더니 미달인 것은 다른 이야기다.

---

## 9. 덮어쓰기 — 방식보다 앞서는 규칙

### 9.1 결측 이력

시계열(`history.json`, 현재 68일 · 7/1~9/6) 전 구간에서 가격이 비었거나 0원이었던 날을 센다.

```
gaps_i = |{ j : g_i[j] ∈ {null, 0}  또는  d_i[j] ∈ {null, 0} }|     (전 날짜축)

시계열 자체가 없으면 gaps_i = 날짜축 길이   (전부 결측으로 본다)
```

`gaps_i > 0` 이면 **방식과 무관하게** 색을 덮어쓴다.

```
              ⎧ stale     오늘 휘발유·경유가 모두 있음   → 과거 미신고
signal_i  :=  ⎨
              ⎩ unknown   오늘 값이 없음                 → 가격정보 없음
```

세 기준(통합·휘발유·경유)의 색과 `adjusted.signal` 까지 같이 덮는다.

**왜 나누나.** `unknown` 은 지금 확인할 일이고 `stale` 은 신고 이력을 따질 일이라
한 칸에 묶으면 무엇을 확인해야 하는지 흐려진다. 하루치만 보고 판정하면
"어제는 1위, 오늘은 미상"처럼 오락가락하는데, 신고를 거른 이력이 있으면 그 값을
믿고 순위를 매기기 어렵다는 뜻이라 판정에서 빼되 구분해 둔다.

> 이 규칙은 **한 번이라도** 걸렀으면 걸린다. 68일 중 하루만 비어도 `stale` 이다.
> 구간을 좁히거나 허용 일수를 두는 완화는 아직 넣지 않았다 — HANDOVER §10 참고.

### 9.2 명단에서 빠진 곳

`gs_station.active = false` 인 곳은 `npm run supabase:pull` 단계에서 명단 자체에서
빠진다. 판정 대상이 아니다.

---

## 10. 유종 단독 기준

화면에서 통합 / 휘발유 / 경유를 고를 수 있다. 집계는 셋을 미리 다 계산해 싣는다.

```
값       x^gasoline(r) = r.g  (>0)          x^diesel(r) = r.d  (>0)
모집단   P^gasoline_s , P^diesel_s          ← 그 유종을 파는 곳만
```

나머지는 §3 과 완전히 같다 — 같은 `N_s`, 같은 조밀 순위, 같은 커트라인, 같은 계수.

**보정은 유종 단독에 적용되지 않는다.** 선정이 합계로 이뤄졌으므로 보정도 합계
위에서만 정의된다. 보정 모드로 두어도 유종 단독 보기는 언제나 방식 A(순위 기준)이고,
화면 문구도 그때만 순위 기준으로 바뀐다.

---

## 11. 기준 충족 일수 (compliance)

하루치 신호등은 그날 사정에 따라 흔들린다. 한 달치 성적으로 꾸준함을 본다.

날짜별 색은 **그날의 시세**로 다시 낸다. 오늘 기준선을 과거에 소급하면 유가가
전체적으로 오르내린 것까지 개별 주유소 탓으로 보인다.

```
그날 τ 에 대해
  B_s(τ)  = 그날 그 지역 적합 커트라인
  B'_s(τ) = 그날 그 지역 근접 커트라인

           ⎧ g      x_i(τ) ≤ B_s(τ)
  s_i(τ) = ⎪ y      B_s(τ) < x_i(τ) ≤ B'_s(τ)
           ⎨ r      x_i(τ) > B'_s(τ)
           ⎩ null   값이 없음
```

집계 구간은 `COMPLIANCE_FROM = 20260801` 부터 최신일까지.

```
greenDays   = |{ τ ≥ 20260801 : s_i(τ) = g }|
yellowDays  = |{ τ : s_i(τ) = y }|
redDays     = |{ τ : s_i(τ) = r }|
missingDays = |{ τ : s_i(τ) = null }|
```

> **이 값은 언제나 방식 A 기준이다.** 판정 방식을 보정으로 바꿔도 기준 충족 일수는
> 순위 기준 그대로다. 과거 각 날짜의 보정 판정을 다시 계산하지는 않기 때문이다.

---

## 12. 설정값이 어디에 꽂히는가

관리 화면(`#/admin`) → Supabase `gs_config` → `data/thresholds.json` →
**저장하는 즉시** 현황판에 반영된다. 현황판이 설정을 읽어 그 자리에서 다시 판정하기
때문이다 — `src/lib/judge.ts`. 다만 파일에 박힌 값(`board-*.json`, 시계열의 하루치
판정)은 다음 집계에서 바뀐다.

| 화면 항목 | 키 | 기본 | 식에서 | 바꾸면 |
| --- | --- | --- | --- | --- |
| 서울·경기 적합 순위 | `rankGreenMetro` | 10 | `N_s` (서울·경기) | 적합 자리 수 자체 |
| 그 외 적합 순위 | `rankGreenDefault` | 5 | `N_s` (그 외) | 위와 같음 |
| 근접 배수 | `rankYellowFactor` | 2 | `f` → `Y_s = N_s·f` | 근접 폭 |
| 판정 방식 | `judgeMode` + `adjustedCombine` | `rank` + `both` | §4·6·7·8 | 어느 식으로 색을 낼지 |
| 이탈률 적합선 | `driftGreen` | 0 (0%) | `Sig_drift` 의 초록 상한 | 방식 C·D |
| 이탈률 근접선 | `driftYellow` | 0.01 (1%) | `Sig_drift` 의 노랑 상한 | 방식 C·D |

**네 방식의 코드값 대응**

| 화면 | `judgeMode` | `adjustedCombine` |
| --- | --- | --- |
| 기존 방식 | `rank` | (무시된다) |
| 보정 · 순위 | `adjusted` | `rank` |
| 보정 · 이탈률 | `adjusted` | `drift` |
| 보정 · 둘 다 | `adjusted` | `both` |

집계는 **언제나 방식 A 와 보정 재료를 모두 계산해 함께 싣는다.** `judgeMode` 는
어느 쪽을 기본으로 보일지만 정한다. 그래서 관리 화면의 견주기 표가 저장 없이도
네 방식을 즉시 세어 보여줄 수 있고, 이탈률 임계값을 만지면 바로 반영된다.

화면 전환은 `applyJudgeMode()` 한 곳에서 `signal` 을 갈아 끼운다. 지도·목록·요약·
거르기가 저마다 방식을 따지게 두면 한 군데는 반드시 어긋나기 때문이다.

`data/round-windows.json` 은 별개 파일이다. 차수별 기준기간(`w`)을 덮어쓰며
`npm run baseline` 을 다시 돌려야 반영된다.

---

## 13. 수치 예제

### 13.1 하이테크에너지㈜단양하이테크주유소 — 방식에 따라 근접 ↔ 적합

```
기준일 2026-09-06 · 충북 단양군 · 8차 (기간 확인됨)

휘발유 1,814 · 경유 1,788        →  x = 3,602
기준지역 s = 충북,  N = 5,  f = 2,  n = 648
```

**방식 A — 기존 순위**

```
U_충북 의 5번째 값        B  = 3,600
ρ(3602, U_충북)           = 6
Sig_rank(6, 5, 2, 648)    = 6 ≤ 5?  아니오 → 6 ≤ 10?  예 → yellow

c₀ = round_4(3602 / 3600) = round_4(1.000555…) = 1.0006
     x > B 이고 1.0006 > 1 이라 걸어 잠글 것 없음      c = 1.0006
```

→ **근접**

**방식 B — 보정 · 순위**

```
U_충북  = 3558 · 3594 · 3596 · 3598 · [3600] · 3602 · …     ← 5번째가 커트라인
Ũ_충북  =        3594 · 3596 · 3598 · 3600 · [3602] · …     ← 3,558원 한 자리가 빠졌다

제외 109곳을 뺀 Ũ_충북 에서
  B̃ = 3,602      (커트라인이 한 칸 밀려 올라왔다)
  ρ̃ = 5
Sig_rank(5, 5, 2, |Ũ|)  = 5 ≤ 5 → green
c̃ = round_4(3602 / 3602) = 1     (x = B̃ 라 걸어 잠그지 않는다)
```

→ **적합**. 커트라인 위에 정확히 얹혀 계수가 1 이다.

**방식 C — 보정 · 이탈률**

```
P0 = 3,650.58     (2026-07 한 달 31일 평균)
M0 = 3,760        (2026-07 충북 시장 중앙값)
M1 = 3,710        (2026-09-06 충북 시장 중앙값)

Ê  = 3650.58 × (3710 / 3760) = 3650.58 × 0.9867021… = 3,602.04
δ  = (3602 − 3602.04) / 3602.04 = −0.0000111  →  −0.00001

Sig_drift(−0.00001)  =  −0.00001 ≤ 0 → green
```

→ **적합**. 7월보다 약 49원 내렸는데 시장도 딱 그만큼 내렸다. 시장을 정확히 따라갔다.

**방식 D — 둘 다**

```
worse(green, green) = green
```

→ **적합**

**정리** — 같은 주유소가 A 근접 / B·C·D 적합.

### 13.2 해맞이주유소 — 순위와 이탈률이 갈리는 경우

```
기준일 2026-09-06 · 제주 서귀포시 · 8차

휘발유 1,867 · 경유 1,845        →  x = 3,712
s = 제주,  N = 5,  f = 2,  n = 189,  B = 3,668
```

```
A   ρ = 17    17 > 10 → red        c = round_4(3712 / 3668) = 1.012
B   ρ̃ = 17   (제외분이 이 지역엔 영향 없음) → red
C   P0 = 3,730.45 · M0 = 3,798 · M1 = 3,760
    Ê = 3730.45 × (3760 / 3798) = 3730.45 × 0.9899947… = 3,693.13
    δ = (3712 − 3693.13) / 3693.13 = 0.005110
    0 < 0.005110 ≤ 0.01 → yellow
D   worse(red, yellow) = red
```

→ A 초과 / B 초과 / **C 근접** / D 초과.

**두 방식이 무엇을 묻는지가 다르다.** 제주 17위는 오늘 기준으로 싼 축이 아니다.
그러나 7월 대비 시장이 1.0% 내리는 동안 이 주유소는 0.5% 만 내렸다 — 시장을
0.51% 만큼 덜 따라간 것이지 값을 올린 것은 아니다. 순위는 "지금 싼가"를,
이탈률은 "약속을 지켰나"를 묻는다.

---

## 14. 네 방식의 실제 차이 (2026-09-06 · 472곳)

| 방식 | 적합 | 근접 | 초과 | 가격정보 없음 | 과거 미신고 |
| --- | ---: | ---: | ---: | ---: | ---: |
| A 기존 방식 | 94 | 61 | 293 | 4 | 20 |
| B 보정 · 순위 | 101 | 62 | 285 | 4 | 20 |
| C 보정 · 이탈률 | **216** | 200 | 32 | 4 | 20 |
| D 보정 · 둘 다 | **59** | 104 | 285 | 4 | 20 |

`가격정보 없음 4` · `과거 미신고 20` 은 네 방식에서 모두 같다 — §9 가 방식보다
앞서기 때문이다. **현재 설정은 C (보정 · 이탈률)** 이다.

읽는 법:

- **A → B** 는 제외 109곳을 뺀 효과다. 적합 +7 — 순위가 몇 칸 움직이는 정도다.
- **B → C** 가 가장 크다. 순위를 안 보고 약속 이행만 묻기 때문이다. 명단 대부분이
  선정 이후 시장 이상으로 올리지는 않았으나, 오늘 기준 상위권도 아니다.
- **D** 는 A·B 보다도 적합이 적다. 두 관문을 모두 통과해야 하는데 그 둘이 서로 다른
  것을 묻기 때문에 통과 집합의 교집합이 좁다.

---

## 15. 경계와 예외

| 상황 | 결과 |
| --- | --- |
| 한 유종만 파는 곳 | 합계가 없다 → 통합 기준 `unknown`. 그 유종 단독 기준으로는 판정된다 |
| 서로 다른 값이 `N_s` 보다 적음 | `B_s` = 그 지역 최고가 → 전원 적합. 시·도 단위에선 안 일어남 |
| `n_s < 2` | `unknown`. 시·도 단위에선 안 일어남 |
| `B_s ≤ 0` | 계수 `null` |
| 기준선 없음 (`baseline.json` 부재) | `adjusted = null`, 보정 방식 전원 `unknown`. 집계가 경고를 낸다 |
| 그 주유소 `P0` 없음 (기준기간 원본 결측) | `adjusted = null` → 보정 방식에서 `unknown` |
| `M0` 또는 `M1` 없음 | 같음 |
| 명단 주소의 시·도가 개편으로 어긋남 | **오피넷 소재지가 정답이다.** 명단 기준으로 통계를 찾으면 존재하지 않는 지역이 되어 전부 미상으로 떨어진다 |
| 일반구(창원 마산합포구 등) | 지도 드릴다운 한 단계에만 쓴다. 판정은 언제나 시·도 단위 |
| 1~7차 이탈률 | 기준기간이 추정(202607)이라 실제보다 후하다. 화면에서 '추정'으로 밝힘 |
| 그날 원본 업로드가 덜 끝남 | `COMPLETE_RATIO = 0.8` 미만이면 그 날은 쓰지도 저장하지도 않는다 |

---

## 16. 이 문서를 고칠 때

식을 바꾸면 **코드가 먼저**다. 이 문서는 코드를 따라온다. 특히:

- `N`·`f` 는 `data/thresholds.json` 이 이기고, 코드 기본값은 그 파일이 없을 때만 쓰인다.
- 차수별 기준기간을 알게 되면 `data/round-windows.json` 에 적고 `npm run baseline`.
  `CONFIRMED_ROUNDS` 에도 그 차수를 넣어야 화면의 '추정' 표시가 걷힌다.
- 과거 날짜의 판정을 새 규칙으로 다시 내려면 `npm run backfill` 로 전 구간을 다시
  돌려야 한다. 짧은 구간만 돌리면 그 하루만 옛 규칙으로 남는다 — HANDOVER §7 참고.
